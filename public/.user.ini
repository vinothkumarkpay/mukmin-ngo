; Applied on many shared hosts (cPanel) when placed next to the app entry point.
; After uploading, wait a few minutes or restart PHP-FPM if limits do not apply immediately.
upload_max_filesize = 20M
post_max_size = 64M
max_file_uploads = 50
memory_limit = 256M
max_execution_time = 120
